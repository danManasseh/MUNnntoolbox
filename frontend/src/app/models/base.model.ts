/**
 * This is a base model. Should be extended by every model.
 * Right now this is empty. We may need it later for the abstraction.
 * Even if it is empty, it does not hurt.
 */
export default class BaseModel {
  id?:string;
  name?: string;
  user_id?:string;
  userId?:string;
}
